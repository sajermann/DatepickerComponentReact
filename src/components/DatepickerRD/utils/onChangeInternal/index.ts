export function onChangeInternal(date: Date | null) {
  // setStartDate(date);
  // const dataVerify = date ? date.toISOString() : '';
  // if (rest.onChange) {
  //   const t = {
  //     target: {
  //       value: dataVerify,
  //       id: rest.id,
  //     },
  //   } as ChangeEvent<HTMLInputElement>;
  //   rest.onChange(t);
  // }
}
